{
  "name": "novacoin-web",
  "version": "1.0.0",
  "private": true,
  "description": "NovaCoin — sitio web con onboarding KYC",
  "scripts": {
    "start": "node server.js",
    "test:validators": "node tests/validators.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:validators && npm run test:e2e",
    "refresh-lists": "node scripts/refresh-lists.js"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
